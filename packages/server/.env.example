GITHUB_ORG_ID=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# generate with `openssl rand -hex 24`
API_SECRET=
# generate with `openssl rand -hex 24`
ALCHEMY_PASSWORD=
# generate with `openssl rand -hex 24`
ALCHEMY_STATE_TOKEN=